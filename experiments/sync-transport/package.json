{
  "name": "sync-transport-playground",
  "private": true,
  "type": "module",
  "description": "Decoupled playground validating lode's Phase D sync transport (raw loro-crdt + a self-written minimal sync loop). NOT production code; not part of the npm workspace. Resolves loro-crdt/vitest from the monorepo root node_modules via Node's upward resolution.",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "loro-crdt": "^1.11.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.9"
  }
}
